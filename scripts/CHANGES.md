# Meteor Client Chinese Localization Patches

## Overview

This document describes all modifications made in this branch to the original Meteor Client source code for Chinese localization support. All code and documentation were generated by AI.

---

## 1. Translation Runtime Infrastructure

### Modified files:

- `src/main/java/meteordevelopment/meteorclient/utils/Utils.java`
- `src/main/java/meteordevelopment/meteorclient/MeteorClient.java`
- `src/main/java/meteordevelopment/meteorclient/commands/Command.java`
- `src/main/java/meteordevelopment/meteorclient/gui/themes/meteor/MeteorGuiTheme.java`

### Changes

**Utils.java** — Three static members added:

| Member | Purpose |
|--------|---------|
| `TRANSLATIONS` (`HashMap<String,String>`) | In-memory translation map |
| `loadTranslations()` | Reads `zh_cn_full.json` from classpath, populates the map |
| `tr(String)` | Returns translated string, falls back to English |
| `trIfPresent(String)` | Returns translated string or null |

**MeteorClient.java** — `onInitializeClient()` now calls `Utils.loadTranslations()` BEFORE `ReflectInit.registerPackages()`, so that modules/settings are constructed with translated strings from the start.

**Command.java** — Constructor wraps `title` and `description` with `Utils.tr()`.

**MeteorGuiTheme.java** — The following widget factory methods wrap their String parameter with `Utils.trIfPresent()`: `window()`, `label()`, `horizontalSeparator()`, `button()`, `confirmedButton()`, `tooltip()`, `section()`. This ensures all GUI text elements display Chinese without needing per-call changes.

---

## 2. Translation Data

### Added files:

- `scripts/translations.json` — Master translation table (3210 entries)
- `src/main/resources/assets/meteor-client/lang/zh_cn_full.json` — Runtime language file (generated)

### Modified files:

- `scripts/translate.py`
- `.github/workflows/translate.yml`

### Workflow

1. `translate.py` scans all 949 Java source files, extracts 3213 unique English strings via regex patterns.
2. Loads `translations.json` (hardcoded translations, built offline by AI).
3. For any string not found in the table, falls back to Google Translate API.
4. Writes output to `zh_cn_full.json`.

### Translation Statistics

| Source | Count |
|--------|-------|
| Hardcoded table (direct) | ~2680 |
| Pattern-based rules (prefix/suffix) | ~510 |
| Google API fallback | ~22 |
| Still English (proper nouns) | ~4 |

### Pattern Rules Applied

Prefixes: Automatically→自动, Disables→禁用, Enables→启用, Allows→允许, Prevents→防止, Shows→显示, Renders→渲染, How many→多少个, Color of→颜色, etc.

Suffixes: Mode→模式, Speed→速度, Color→颜色, Range→范围, Type→类型, etc.

---

## 3. Font & CJK Rendering

### Modified files:

- `src/main/java/meteordevelopment/meteorclient/renderer/text/Font.java`
- `src/main/java/meteordevelopment/meteorclient/renderer/text/CustomTextRenderer.java`
- `src/main/java/meteordevelopment/meteorclient/systems/config/Config.java`
- `src/main/java/meteordevelopment/meteorclient/systems/hud/Hud.java`

### Changes

**Config.java** — `customFont` default reverted to `true` (original Meteor default). Setting it to `false` caused text misalignment because `VanillaTextRenderer` and `CustomTextRenderer` use different scaling calculations and font metrics.

**Hud.java** — Same `customFont` default reverted to `true`.

**Font.java** — Added `CJK Unified Ideographs` glyph range (U+4E00–U+9FFF, 20992 codepoints) to STB TrueType packing. When a CJK-capable font is loaded, these glyphs are rendered through the custom renderer.

**CustomTextRenderer.java** — Added `containsCJK()` heuristic: if text contains characters in CJK ranges (U+3400–U+4DBF, U+4E00–U+9FFF, U+F900–U+FAFF) or any `Character.isIdeographic()` codepoint, the render call is delegated to `VanillaTextRenderer.INSTANCE`. This applies to both `getWidth()` and `render()`.

This hybrid approach:
- English/Latin text → custom font (Comfortaa, preserves original visual style).
- Chinese/CJK text → vanilla Minecraft font (correct CJK glyphs, no boxes or spaces).

---

## 4. Build & CI

### Modified files:

- `.github/workflows/translate.yml`

Workflow is manually triggered (`workflow_dispatch`). Steps:
1. Checkout
2. Setup Python
3. `pip install deep-translator`
4. `python scripts/translate.py` (generates `zh_cn_full.json` from `translations.json`)
5. Setup JDK 25 + Gradle
6. `./gradlew build`
7. Upload JAR artifact

---

## 5. Scripts

### Added:

- `scripts/translations.json` — 3210-entry hardcoded translation table
- `scripts/translate.py` — Extraction + translation + output generation
- `scripts/CHANGES.md` — This document

---

## Disclaimer

**All code and documentation in this branch were generated entirely by AI language models. No human wrote any of the Java or Python source files. The AI was instructed with the goal of enabling Chinese language support for the Meteor Client Minecraft utility mod.**

---

# Meteor Client 中文汉化补丁说明

## 概述

本文档描述了为实现中文汉化而对 Meteor Client 原始源码所做的全部修改。所有代码和文档均由 AI 生成。

---

## 1. 翻译运行时基础设施

### 修改的文件

- `src/main/java/meteordevelopment/meteorclient/utils/Utils.java`
- `src/main/java/meteordevelopment/meteorclient/MeteorClient.java`
- `src/main/java/meteordevelopment/meteorclient/commands/Command.java`
- `src/main/java/meteordevelopment/meteorclient/gui/themes/meteor/MeteorGuiTheme.java`

### 修改内容

**Utils.java** — 添加了三个静态成员：

| 成员 | 用途 |
|------|------|
| `TRANSLATIONS` (`HashMap<String,String>`) | 内存翻译映射表 |
| `loadTranslations()` | 从 classpath 读取 `zh_cn_full.json`，填充映射表 |
| `tr(String)` | 返回翻译后的字符串，无翻译时回退为英文 |
| `trIfPresent(String)` | 返回翻译后的字符串，无翻译时返回 null |

**MeteorClient.java** — `onInitializeClient()` 在 `ReflectInit.registerPackages()` 之前调用 `Utils.loadTranslations()`，确保模块/设置在构造时就使用翻译后的字符串。

**Command.java** — 构造函数将 `title` 和 `description` 用 `Utils.tr()` 包装。

**MeteorGuiTheme.java** — 以下 widget 工厂方法将其 String 参数用 `Utils.trIfPresent()` 包装：`window()`, `label()`, `horizontalSeparator()`, `button()`, `confirmedButton()`, `tooltip()`, `section()`。这样所有 GUI 文本元素都能显示中文，无需逐处修改。

---

## 2. 翻译数据

### 新增的文件

- `scripts/translations.json` — 主翻译表（3210 条）
- `src/main/resources/assets/meteor-client/lang/zh_cn_full.json` — 运行时语言文件（生成产物）

### 修改的文件

- `scripts/translate.py`
- `.github/workflows/translate.yml`

### 工作流程

1. `translate.py` 扫描全部 949 个 Java 源文件，通过正则表达式提取出 3213 条唯一英文字符串。
2. 加载 `translations.json`（由 AI 离线构建的硬编码翻译表）。
3. 对于表中未找到的字符串，回退到 Google Translate API。
4. 输出写入 `zh_cn_full.json`。

### 翻译统计

| 来源 | 数量 |
|------|------|
| 硬编码表（直接翻译） | ~2680 |
| 模式匹配规则（前缀/后缀） | ~510 |
| Google API 回退 | ~22 |
| 保留英文（专有名词） | ~4 |

### 使用的模式规则

前缀：Automatically→自动, Disables→禁用, Enables→启用, Allows→允许, Prevents→防止, Shows→显示, Renders→渲染, How many→多少个, Color of→颜色, 等。

后缀：Mode→模式, Speed→速度, Color→颜色, Range→范围, Type→类型, 等。

---

## 3. 字体与中文渲染

### 修改的文件

- `src/main/java/meteordevelopment/meteorclient/renderer/text/Font.java`
- `src/main/java/meteordevelopment/meteorclient/renderer/text/CustomTextRenderer.java`
- `src/main/java/meteordevelopment/meteorclient/systems/config/Config.java`
- `src/main/java/meteordevelopment/meteorclient/systems/hud/Hud.java`

### 修改内容

**Config.java** — `customFont` 默认值恢复为 `true`（Meteor 原始默认值）。设为 `false` 会导致文字错位，因为 `VanillaTextRenderer` 和 `CustomTextRenderer` 使用不同的缩放算法和字体度量。

**Hud.java** — 同上，`customFont` 默认值恢复为 `true`。

**Font.java** — 添加了 `CJK 统一表意文字` 字形范围（U+4E00–U+9FFF，共 20992 个码点）到 STB TrueType 打包中。当加载了支持中文的字体时，这些字形将通过自定义渲染器渲染。

**CustomTextRenderer.java** — 添加了 `containsCJK()` 检测：如果文本包含 CJK 范围的字符（U+3400–U+4DBF、U+4E00–U+9FFF、U+F900–U+FAFF）或任何 `Character.isIdeographic()` 码点，则渲染调用委托给 `VanillaTextRenderer.INSTANCE`。此逻辑同时作用于 `getWidth()` 和 `render()`。

这种混合方案：
- 英文/拉丁文本 → 自定义字体（Comfortaa，保留原始视觉风格）。
- 中文/CJK 文本 → 原版 Minecraft 字体（正确显示中文，没有方框或空格）。

---

## 4. 构建与持续集成

### 修改的文件

- `.github/workflows/translate.yml`

工作流为手动触发（`workflow_dispatch`）。步骤：
1. 检出代码
2. 设置 Python
3. `pip install deep-translator`
4. `python scripts/translate.py`（从 `translations.json` 生成 `zh_cn_full.json`）
5. 设置 JDK 25 + Gradle
6. `./gradlew build`
7. 上传 JAR 产物

---

## 5. 脚本

### 新增

- `scripts/translations.json` — 3210 条硬编码翻译表
- `scripts/translate.py` — 提取 + 翻译 + 输出生成
- `scripts/CHANGES.md` — 本文档

---

## 声明

**本分支中的所有代码和文档均由 AI 语言模型完全生成。没有任何人类编写了任何 Java 或 Python 源文件。AI 的指令目标是实现 Meteor Client Minecraft 实用模组的中文语言支持。**
